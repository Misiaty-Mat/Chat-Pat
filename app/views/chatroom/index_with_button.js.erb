$('#messagesContainer').prepend('<%= j render @messages.reverse %>');
<% if @messages.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @messages %>');
  $('.pagination').hide();
  $('.pagination, #loadMoreMessages').prependTo('#messagesContainer');
<% else %>
  $('.pagination, #loadMoreMessages').remove();
<% end %>